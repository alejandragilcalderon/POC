# Copy to .env.local (gitignored). Do not commit real values.
# Or export variables in your shell before: npm run build | npm run start | npm run dev

# NASA — server-only (never use NEXT_PUBLIC_ for this)
NASA_APOD_API_KEY=

# Optional: skip scripts/verify-env.mjs (e.g. CI compile-only) — not recommended for deploys
# SKIP_ENV_VALIDATION=1
