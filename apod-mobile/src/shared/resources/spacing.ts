/**
 * Layout tokens — padding, margins, and gaps used across screens.
 */
export const spacing = {
  /** Default horizontal inset for screen headers, scroll content, and full-bleed cards. */
  screenPaddingX: 18,
  /** Standard gap below a title block or intro. */
  sectionGapMd: 12,
  /** Tighter gap below compact headers. */
  sectionGapSm: 8,
  /** Extra inset at the top of tab screen content (below status bar / safe area). */
  tabScenePaddingTop: 12,
} as const;
